import { HighligtsDirective } from './highligts.directive';

describe('HighligtsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligtsDirective();
    expect(directive).toBeTruthy();
  });
});
